// Types for static image imports. Next generates these in next-env.d.ts, but that file is
// gitignored and only appears after a build, so CI needs the reference committed.
/// <reference types="next/image-types/global" />
